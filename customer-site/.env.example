VITE_API_BASE_URL=http://localhost:4000/api/v1
# Razorpay's public key_id is not needed here — the checkout flow fetches it dynamically from
# POST /orders/razorpay so the frontend never needs its own copy of it.
